.sidebar {
  display: none;

  @media (--md) {
    display: flex;
    width: 30%;
    max-width: 200px;
    padding: 0 10px 10px 20px;

    .tag-list {
      position: sticky;
      top: 90px;
      margin: 0;
      padding: 0;

      a {
        color: var(--color-nav);
      }
    }
  }
}